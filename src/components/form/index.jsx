import React from 'react';

const Form = () => {
	return (
		<form>
			<input type={'text'} className="search-box" />
			<input type={'submit'} value="Search" />
		</form>
	);
};

export default Form;
